type User = {
  userId: number;
  // firstName: string;
  // lastName: string;
  // avatar: string;
  // description: string;
  // phoneNumber: string;
  showConfidentialInformation: boolean;
  // birthDate: Date;
  email: string;
  login: string;
  // registrationDate: Date;
  isAdmin: boolean;
};

export default User;
